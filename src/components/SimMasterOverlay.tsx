// src/components/SimMasterOverlay.tsx
// SimMaster v3 — Floating overlay removed; sidebar panel is now the primary UI.

import React from 'react';

interface SimMasterOverlayProps { enabled: boolean; }

const SimMasterOverlay: React.FC<SimMasterOverlayProps> = () => null;

export default SimMasterOverlay;
